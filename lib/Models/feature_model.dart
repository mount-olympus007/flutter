class FeatureModel {
  int id;
  String featureName;
  String featureDescription;
  int projectId;
  FeatureModel(
      this.id, this.featureName, this.featureDescription, this.projectId);
}
